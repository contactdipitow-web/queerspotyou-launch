import { Pressable, StyleSheet, Text, View } from 'react-native';
import { router } from 'expo-router';
import type { Venue } from '@/types';
import { colors, radius } from '@/theme';
export function VenueCard({ venue }: { venue: Venue }) { return <Pressable onPress={() => router.push({ pathname: '/place/[id]', params: { id: venue.id } })} style={styles.card}><View style={styles.icon}><Text style={{ fontSize: 20 }}>✦</Text></View><View style={{ flex: 1 }}><Text style={styles.title}>{venue.name}</Text><Text style={styles.meta}>{venue.category_label ?? venue.city}{venue.distance_meters != null ? ` · ${Math.round(venue.distance_meters)} m` : ''}</Text><Text numberOfLines={2} style={styles.address}>{venue.address_line}</Text></View><Text style={styles.arrow}>›</Text></Pressable>; }
const styles = StyleSheet.create({ card: { flexDirection: 'row', alignItems: 'center', gap: 12, borderRadius: radius.md, padding: 14, backgroundColor: colors.panel, borderWidth: 1, borderColor: colors.line }, icon: { width: 42, height: 42, borderRadius: 14, backgroundColor: colors.purple, alignItems: 'center', justifyContent: 'center' }, title: { color: colors.ivory, fontWeight: '900', fontSize: 17 }, meta: { color: colors.mint, marginTop: 3, fontWeight: '800', fontSize: 12 }, address: { color: colors.muted, marginTop: 4, lineHeight: 18 }, arrow: { color: colors.fuchsia, fontSize: 30, fontWeight: '300' } });
